/* Site settings.

   Change a value below, save the file, and push. Nothing else needs to
   change, and the new setting applies on the next page load.
   True turns something on, false turns it off. */

const SETTINGS = {

  /* The equation line on each formula page, for example
     "KE = ½ · m · v²" shown above the input fields.
     true  = visitors see the equation.
     false = visitors see only the inputs and the answer. */
  showEquation: true,

  /* A line shown at the bottom of every calculator.
     Set it to '' to remove it everywhere. */
  missionLine: 'Our goal is to make useful knowledge and tools available to everyone',

};
